[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy && npm run start -- -p $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
