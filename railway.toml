[build]
builder = "nixpacks"

[deploy]
startCommand = "bash scripts/baseline.sh && npx tsx scripts/create-superadmin.ts || true && next start -H 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
