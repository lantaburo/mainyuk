[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy || true && next start -H 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
